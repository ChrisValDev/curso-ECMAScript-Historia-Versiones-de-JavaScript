const hello = '           Hello world!                ';
console.log(hello.trimStart());
console.log(hello.trimEnd());

// Los métodos trimStart() y trimEnd() son métodos de las cadenas de texto en JavaScript que se utilizan para eliminar espacios en blanco (como espacios, tabulaciones y saltos de línea) al principio o al final de una cadena de texto, respectivamente.