let numbers = [1, 2, 5, 32, 76, 8, 4, 3];

console.log(numbers.includes(90));

const list = ['Oscar', 'Pedro', 'Federico']

console.log(list.includes('Oscar'));

// Metodo includes evuelve true o false